import 'package:flutter/material.dart';

/// Widget that shall be shown when State has been transitioned and there is a time.
class SaveWidget extends StatefulWidget {
  SaveWidgetState createState() => new SaveWidgetState();
}

class SaveWidgetState extends State<SaveWidget> {
  @override
  Widget build(BuildContext context) {
    return null;
  }
}
